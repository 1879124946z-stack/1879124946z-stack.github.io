# 学生下载版

学生在公开入口填写学号，选择实验，完成后点击下载 CSV，并把文件发送给老师。每次实验生成独立文件，包含学号、项目、版本、时间、唯一记录编号、逐题数据和汇总 JSON。CSV 使用 UTF-8 BOM。

此版本不调用后台，不自动上传结果。关闭或刷新页面会丢失尚未下载的数据。学生应在离开结果页之前下载并确认文件保存成功。现有实验初始化继续使用 pre；没有新增前后测选择。

源码位于 source/github-src/download.tsx 和 source/github-src/csv.mjs。进入 source，安装依赖后运行 `pnpm exec vite build --config vite.download.config.ts`。将 download-dist 内容以及 public/tasks 目录发布到 Pages 根目录。资源使用相对路径，也支持项目子目录。

请勿向此公开仓库上传学生结果、密码、密钥或私有数据库。
